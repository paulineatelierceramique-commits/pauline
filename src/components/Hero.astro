---
import type { ImageMetadata } from 'astro';

interface Props {
  titre: string;
  lienLabel?: string;
  lienHref?: string;
  image: ImageMetadata;
  imageAlt: string;
  
}

const {
  titre,
  lienLabel = "Découvrez mes services",
  lienHref = `${import.meta.env.BASE_URL}services`,
  image,
  imageAlt
} = Astro.props;
---

<div class="hero">
  <div class="text-hero">
    <div class="main-text">
      <h1>{titre}</h1>
    </div>

    <div class="secondary-text">
      <slot />
    </div>

    <a href={lienHref} class="bouton-fonce">{lienLabel}</a>
  </div>

  <div class="hero-img">
    <img
      src={image.src}
      alt={imageAlt}
      width={image.width}
      height={image.height}
      loading="lazy"
    />
  </div>
</div>

<style>
@keyframes zoomIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.hero {
  background-color: var(--color-bg1);
  display: flex;
  flex: 1;
  margin: 0;
  padding: 0 10%;
  justify-content: space-between;
  align-items: center;
}

.text-hero {
  display: flex;
  flex-direction: column;
  align-items: start;
  max-width: 100%;
  margin: 0;
  padding: 5%;
  color: var(--color-text);
  animation: zoomIn 0.8s ease forwards;
}

.hero-img {
  display: flex;
  animation: zoomIn 0.8s ease forwards;
}

.hero-img img {
  max-width: 700px;
  width: auto;
  height: auto;
  margin: 0 20px;
  border-radius: 12px;
}


.secondary-text :global(p) {
  margin-bottom: 1rem;
}

.secondary-text :global(a) {
  color: inherit;
}

.secondary-text :global(.coordonnees) {
  display: flex;
  flex-direction: column;
  gap: 0.85rem;
  margin-top: 1rem;
}

.secondary-text :global(.coordonnees a) {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  color: inherit;
  text-decoration: none;
}

.secondary-text :global(.coordonnees img) {
  display: block;
  width: 22px;
  height: 22px;
  flex-shrink: 0;
}

@media screen and (max-width: 900px) {
  .hero {
    flex-direction: column;
    align-items: center;
    padding: 0 5%;
    text-align: center;
  }

  .hero-img {
  width: 100%;
}

.hero-img img {
  width: 100%;
  max-width: 500px;
  height: auto;
  margin: 0 auto 20px;
  display: block;
}
  .text-hero {
    width: 100%;
    align-items: center;
    margin-bottom: 20px;
  }

  

  .main-text {
    font-size: clamp(28px, 5vw, 42px);
  }

  .secondary-text {
    font-size: inherit;
  }

  .bouton-fonce {
    display: block !important;
    margin-inline: auto !important;
    text-align: center;
    width: fit-content;
    margin: 10px 0;
  }
}
</style>